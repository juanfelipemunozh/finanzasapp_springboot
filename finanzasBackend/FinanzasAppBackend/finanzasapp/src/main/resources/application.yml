server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finanzas_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: A2121584a.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
